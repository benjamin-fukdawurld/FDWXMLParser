#include "BuildableMessage.h"

using namespace std;

BuildableMessage::BuildableMessage() : AbstractXMLBuildableObject()
                                   /*const string &sourceName,
                                   const string &commandName,
                                   const list<string> destinationNames,
                                   const uint64_t bodySize,
                                   unsigned char *body) :
                                   ,
                                   m_header(sourceName, commandName, BuildableDestination(destinationNames))*/
{

}
